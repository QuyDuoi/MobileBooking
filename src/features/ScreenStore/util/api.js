import {ipAddress} from '../../../services/api';

export const api = {
  StoreGetList: `${ipAddress}getListStore`,
  StoreAdd: `${ipAddress}addStore`,
  StoreDelete: `${ipAddress}deleteStore`,
  StoreUpdate: `${ipAddress}updateStore`,
};

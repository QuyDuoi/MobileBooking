import {ipAddress} from '../../../services/api';

export const api = {
  seviceGetList: `${ipAddress}getListService`,
  seviceAdd: `${ipAddress}addService`,
  seviceDelete: `${ipAddress}deleteService`,
  seviceUpdate: `${ipAddress}updateService`,
};
